services:
  web:
    # Local development only. Coolify reaches this service through its private
    # Docker network and reverse proxy, so the production compose file must not
    # reserve a host port.
    ports:
      - "8000:8000"
